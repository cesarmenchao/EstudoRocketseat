
        // Operadores Aritimeticos
        // Multiplicação
        console.log(25 * 5)
        // divisão
        console.log(125/5)
        // soma
        console.log(25+100)
        // subtração
        console.log(125-100)

        // resto da divisão (remainder)
        let remainder 
        remainder = 11 % 3 //Pega oque sobra de uma divisão
        console.log(remainder)
        // incremento ++
        let increment = 0
        increment++ 
        console.log(increment)
        // decremento --
        let decrement = 0 
        console.log(--decrement)// se usar antes de chamar a variavel ele decrementa antes, se passar depois ele vai printar o valor da variavel antes de incrementar
        // exponencial **
        console.log(2 ** 3)//2 elevado a 3




