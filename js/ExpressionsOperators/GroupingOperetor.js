// grouping operador ( )

let semgroup = 2 + 3 * 5; //pela regra aritimetica ele faz a multplicação primeira
console.log(semgroup);
let comgroup = (2 + 3) * 5; //executa o grouping primeiro
console.log(comgroup);
