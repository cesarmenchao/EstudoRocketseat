// Operadores logicos (Logical operators)

// -2 valores booleanos verificados, resuiltara em true or false

let pao = true;
let queijo = false;

// AND $$
console.log(pao && queijo); //false

// OR ||
console.log(pao || queijo); // true

// NOT !
console.log(pao, !queijo); // true true
